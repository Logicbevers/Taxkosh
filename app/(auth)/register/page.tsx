import { RegisterForm } from "@/components/auth/register-form";

export const metadata = {
    title: "Create Account — TaxKosh",
    description: "Create your free TaxKosh account",
};

export default function RegisterPage() {
    return <RegisterForm />;
}
